{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://w3id.org/trace/tro/0.1/document-rooted-in-nodes.schema.json",
    "title": "Document rooted in nodes",
    "description": "The document's root is an object, or an array whose members are all objects.",
    "summary": "The document is an object or an array of objects",
    "type": ["object", "array"],
    "items": {
        "type": "object",
        "errorMessage": {
            "type": "every member of a JSON-LD document written as an array is a node object"
        }
    },
    "errorMessage": {
        "type": "a JSON-LD document is a node object, or an array of node objects"
    }
}
