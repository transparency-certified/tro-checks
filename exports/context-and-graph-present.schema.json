{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://w3id.org/trace/tro/0.1/context-and-graph-present.schema.json",
    "title": "Context and graph present",
    "description": "A TRO declaration is a JSON object that declares an @context and carries its nodes in an @graph.",
    "summary": "A JSON object with an `@context` and an `@graph`",
    "type": "object",
    "required": ["@context", "@graph"],
    "errorMessage": {
        "type": "a TRO declaration is a JSON object",
        "required": {
            "@context": "a TRO declaration declares an @context",
            "@graph": "a TRO declaration carries its nodes in an @graph"
        }
    }
}
