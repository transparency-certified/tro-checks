{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://w3id.org/trace/tro/0.1/graph-well-formed.schema.json",
    "title": "Graph well-formed",
    "description": "Where the document carries an @graph at its root, the value is an object or an array of objects.",
    "summary": "The root `@graph`, if any, holds objects, not bare values",
    "properties": {
        "@graph": {
            "type": ["object", "array"],
            "items": {
                "type": "object",
                "errorMessage": {
                    "type": "every member of an @graph array is a node object"
                }
            },
            "errorMessage": {
                "type": "an @graph holds a node object, or an array of node objects"
            }
        }
    }
}
