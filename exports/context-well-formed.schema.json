{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://w3id.org/trace/tro/0.1/context-well-formed.schema.json",
    "title": "Context well-formed",
    "description": "Where the document carries an @context at its root, the value is null, a string naming an external context, a context object, or an array of those.",
    "summary": "The root `@context`, if any, has a form JSON-LD allows",
    "properties": {
        "@context": {
            "type": ["null", "string", "object", "array"],
            "items": {
                "type": ["null", "string", "object"],
                "errorMessage": {
                    "type": "every member of an @context array is null, a string, or a context object"
                }
            },
            "errorMessage": {
                "type": "an @context is null, a string, a context object, or an array of those"
            }
        }
    }
}
