##############################################################################
#
#   tro-checks  ·  demo 04: trov-terms-known (Tier 1)
#
##############################################################################

----- [1] expectation met: the property name trov:vocabularyVersion is defined by TROV -----

$ report_on instance-property-name-known.jsonld
{
    "@context": { "trov": "https://w3id.org/trace/trov/0.1/" },
    "@graph": [
        {
            "@id": "tro",
            "@type": "trov:TransparentResearchObject",
            "trov:wasAssembledBy": { "@id": "trs", "@type": "trov:TrustedResearchSystem" },
            "trov:vocabularyVersion": "0.1"
        }
    ]
}

wrote tmp/instance-property-name-known.md; Tier 0 met, Tier 1 met

# Report
## Candidate Information
|  |  |
| --- | --- |
| Candidate | `instance-property-name-known.jsonld` |
| Target | Tier 1 — Self-Contained |
| Target declared by | the `--target` option |
## Assessments
### By Representation Tier
| Tier | Name | Description | Status |
| --- | --- | --- | --- |
| 0 | Well-Formed | Valid JSON-LD conforming to the profile TRACE tooling expects | ✅ met |
| 1 | Self-Contained | References within the TRO resolve; its identifiers need not be unique outside it | ✅ met |
| *2* | *Linkable-Data* | *Element identifiers cannot collide with another TRO's; TRO elements can be published* | *not claimed* |
### By Individual Expectation
| Tier | Expectation | Summary | Status |
| --- | --- | --- | --- |
| 0 | base-simple-url | The `@base`, if any, is a simple URL: a host, no user info, dot segments, query or fragment, only URL characters, and a final `/` | ✅ met |
| 0 | base-web-scheme | The `@base`, if any, uses the `https` or `http` scheme | ✅ met |
| 0 | context-well-formed | The root `@context`, if any, has a form JSON-LD allows | ✅ met |
| 0 | disallowed-context-keywords-absent | No keyword in the `@context` other than `@base` | ✅ met |
| 0 | disallowed-node-keywords-absent | No keyword outside the `@context` other than `@context`, `@graph`, `@id` and `@type` | ✅ met |
| 0 | document-rooted-in-nodes | The document is an object or an array of objects | ✅ met |
| 0 | graph-well-formed | The root `@graph`, if any, holds objects, not bare values | ✅ met |
| 0 | ids-and-types-strings | Every `@id` is a string; every `@type` a string or an array of strings | ✅ met |
| 0 | prefix-namespaces-terminated | Every prefix maps to an absolute IRI ending in `#` or `/` | ✅ met |
| 0 | relative-ids-plain | Every relative `@id` is a plain path, with no leading `/`, no `.` or `..` segments, and no `?` or `#` | ✅ met |
| 1 | composition-fingerprinted | The TRO's composition, if any, carries a fingerprint | ✅ met |
| 1 | context-and-graph-present | A JSON object with an `@context` and an `@graph` | ✅ met |
| 1 | hashes-well-formed | The TRO's artifact and fingerprint hashes are well-formed sha256 | ✅ met |
| 1 | tro-assembled-by-trs | The TRO names its assembling system, typed as a TRS | ✅ met |
| 1 | tro-top-level-in-graph | The TRO is a top-level member of the `@graph` | ✅ met |
| 1 | trov-terms-known | Every `trov:` name is one TROV defines | ✅ met |
| *2* | *base-declared* | *The `@context` includes an `@base`* | *not claimed* |
| *2* | *node-ids-present* | *Every node carries an explicit `@id`* | *not claimed* |

----- [2] expectation unmet: the property name trov:vocabularyRelease is not defined by TROV -----

$ report_on instance-property-name-unknown.jsonld
{
    "@context": { "trov": "https://w3id.org/trace/trov/0.1/" },
    "@graph": [
        {
            "@id": "tro",
            "@type": "trov:TransparentResearchObject",
            "trov:wasAssembledBy": { "@id": "trs", "@type": "trov:TrustedResearchSystem" },
            "trov:vocabularyRelease": "0.1"
        }
    ]
}

wrote tmp/instance-property-name-unknown.md; Tier 0 met, Tier 1 unmet

# Report
## Candidate Information
|  |  |
| --- | --- |
| Candidate | `instance-property-name-unknown.jsonld` |
| Target | Tier 1 — Self-Contained |
| Target declared by | the `--target` option |
## Assessments
### By Representation Tier
| Tier | Name | Description | Status |
| --- | --- | --- | --- |
| 0 | Well-Formed | Valid JSON-LD conforming to the profile TRACE tooling expects | ✅ met |
| 1 | Self-Contained | References within the TRO resolve; its identifiers need not be unique outside it | ❌ not met |
| *2* | *Linkable-Data* | *Element identifiers cannot collide with another TRO's; TRO elements can be published* | *not claimed* |
### By Individual Expectation
| Tier | Expectation | Summary | Status |
| --- | --- | --- | --- |
| 0 | base-simple-url | The `@base`, if any, is a simple URL: a host, no user info, dot segments, query or fragment, only URL characters, and a final `/` | ✅ met |
| 0 | base-web-scheme | The `@base`, if any, uses the `https` or `http` scheme | ✅ met |
| 0 | context-well-formed | The root `@context`, if any, has a form JSON-LD allows | ✅ met |
| 0 | disallowed-context-keywords-absent | No keyword in the `@context` other than `@base` | ✅ met |
| 0 | disallowed-node-keywords-absent | No keyword outside the `@context` other than `@context`, `@graph`, `@id` and `@type` | ✅ met |
| 0 | document-rooted-in-nodes | The document is an object or an array of objects | ✅ met |
| 0 | graph-well-formed | The root `@graph`, if any, holds objects, not bare values | ✅ met |
| 0 | ids-and-types-strings | Every `@id` is a string; every `@type` a string or an array of strings | ✅ met |
| 0 | prefix-namespaces-terminated | Every prefix maps to an absolute IRI ending in `#` or `/` | ✅ met |
| 0 | relative-ids-plain | Every relative `@id` is a plain path, with no leading `/`, no `.` or `..` segments, and no `?` or `#` | ✅ met |
| 1 | composition-fingerprinted | The TRO's composition, if any, carries a fingerprint | ✅ met |
| 1 | context-and-graph-present | A JSON object with an `@context` and an `@graph` | ✅ met |
| 1 | hashes-well-formed | The TRO's artifact and fingerprint hashes are well-formed sha256 | ✅ met |
| 1 | tro-assembled-by-trs | The TRO names its assembling system, typed as a TRS | ✅ met |
| 1 | tro-top-level-in-graph | The TRO is a top-level member of the `@graph` | ✅ met |
| 1 | trov-terms-known | Every `trov:` name is one TROV defines | ❌ not met |
| *2* | *base-declared* | *The `@context` includes an `@base`* | *not claimed* |
| *2* | *node-ids-present* | *Every node carries an explicit `@id`* | *not claimed* |
## Details
### Unmet expectation: trov-terms-known
Expectation details: Where a property name or a string value carries the trov: prefix, it is a term TROV defines.
| Found | Where | Expectation not met because |
| --- | --- | --- |
| `{"@id":"tro","@type":"trov:TransparentResearchObject","trov:wasAssembledBy":{"@id":"trs","@type":"trov:TrustedResearchSystem"},"trov:vocabularyRelease":"0.1"}` | `/@graph/0` | not a term TROV defines |

----- [3] expectation met: the @id trov:IncludesAllInputData is defined by TROV -----

$ report_on instance-id-known.jsonld
{
    "@context": { "trov": "https://w3id.org/trace/trov/0.1/" },
    "@graph": [
        {
            "@id": "tro",
            "@type": "trov:TransparentResearchObject",
            "trov:wasAssembledBy": { "@id": "trs", "@type": "trov:TrustedResearchSystem" },
            "trov:hasAttribute": { "@id": "trov:IncludesAllInputData" }
        }
    ]
}

wrote tmp/instance-id-known.md; Tier 0 met, Tier 1 met

# Report
## Candidate Information
|  |  |
| --- | --- |
| Candidate | `instance-id-known.jsonld` |
| Target | Tier 1 — Self-Contained |
| Target declared by | the `--target` option |
## Assessments
### By Representation Tier
| Tier | Name | Description | Status |
| --- | --- | --- | --- |
| 0 | Well-Formed | Valid JSON-LD conforming to the profile TRACE tooling expects | ✅ met |
| 1 | Self-Contained | References within the TRO resolve; its identifiers need not be unique outside it | ✅ met |
| *2* | *Linkable-Data* | *Element identifiers cannot collide with another TRO's; TRO elements can be published* | *not claimed* |
### By Individual Expectation
| Tier | Expectation | Summary | Status |
| --- | --- | --- | --- |
| 0 | base-simple-url | The `@base`, if any, is a simple URL: a host, no user info, dot segments, query or fragment, only URL characters, and a final `/` | ✅ met |
| 0 | base-web-scheme | The `@base`, if any, uses the `https` or `http` scheme | ✅ met |
| 0 | context-well-formed | The root `@context`, if any, has a form JSON-LD allows | ✅ met |
| 0 | disallowed-context-keywords-absent | No keyword in the `@context` other than `@base` | ✅ met |
| 0 | disallowed-node-keywords-absent | No keyword outside the `@context` other than `@context`, `@graph`, `@id` and `@type` | ✅ met |
| 0 | document-rooted-in-nodes | The document is an object or an array of objects | ✅ met |
| 0 | graph-well-formed | The root `@graph`, if any, holds objects, not bare values | ✅ met |
| 0 | ids-and-types-strings | Every `@id` is a string; every `@type` a string or an array of strings | ✅ met |
| 0 | prefix-namespaces-terminated | Every prefix maps to an absolute IRI ending in `#` or `/` | ✅ met |
| 0 | relative-ids-plain | Every relative `@id` is a plain path, with no leading `/`, no `.` or `..` segments, and no `?` or `#` | ✅ met |
| 1 | composition-fingerprinted | The TRO's composition, if any, carries a fingerprint | ✅ met |
| 1 | context-and-graph-present | A JSON object with an `@context` and an `@graph` | ✅ met |
| 1 | hashes-well-formed | The TRO's artifact and fingerprint hashes are well-formed sha256 | ✅ met |
| 1 | tro-assembled-by-trs | The TRO names its assembling system, typed as a TRS | ✅ met |
| 1 | tro-top-level-in-graph | The TRO is a top-level member of the `@graph` | ✅ met |
| 1 | trov-terms-known | Every `trov:` name is one TROV defines | ✅ met |
| *2* | *base-declared* | *The `@context` includes an `@base`* | *not claimed* |
| *2* | *node-ids-present* | *Every node carries an explicit `@id`* | *not claimed* |

----- [4] expectation unmet: the @id trov:IncludesAllOutputData is not defined by TROV -----

$ report_on instance-id-unknown.jsonld
{
    "@context": { "trov": "https://w3id.org/trace/trov/0.1/" },
    "@graph": [
        {
            "@id": "tro",
            "@type": "trov:TransparentResearchObject",
            "trov:wasAssembledBy": { "@id": "trs", "@type": "trov:TrustedResearchSystem" },
            "trov:hasAttribute": { "@id": "trov:IncludesAllOutputData" }
        }
    ]
}

wrote tmp/instance-id-unknown.md; Tier 0 met, Tier 1 unmet

# Report
## Candidate Information
|  |  |
| --- | --- |
| Candidate | `instance-id-unknown.jsonld` |
| Target | Tier 1 — Self-Contained |
| Target declared by | the `--target` option |
## Assessments
### By Representation Tier
| Tier | Name | Description | Status |
| --- | --- | --- | --- |
| 0 | Well-Formed | Valid JSON-LD conforming to the profile TRACE tooling expects | ✅ met |
| 1 | Self-Contained | References within the TRO resolve; its identifiers need not be unique outside it | ❌ not met |
| *2* | *Linkable-Data* | *Element identifiers cannot collide with another TRO's; TRO elements can be published* | *not claimed* |
### By Individual Expectation
| Tier | Expectation | Summary | Status |
| --- | --- | --- | --- |
| 0 | base-simple-url | The `@base`, if any, is a simple URL: a host, no user info, dot segments, query or fragment, only URL characters, and a final `/` | ✅ met |
| 0 | base-web-scheme | The `@base`, if any, uses the `https` or `http` scheme | ✅ met |
| 0 | context-well-formed | The root `@context`, if any, has a form JSON-LD allows | ✅ met |
| 0 | disallowed-context-keywords-absent | No keyword in the `@context` other than `@base` | ✅ met |
| 0 | disallowed-node-keywords-absent | No keyword outside the `@context` other than `@context`, `@graph`, `@id` and `@type` | ✅ met |
| 0 | document-rooted-in-nodes | The document is an object or an array of objects | ✅ met |
| 0 | graph-well-formed | The root `@graph`, if any, holds objects, not bare values | ✅ met |
| 0 | ids-and-types-strings | Every `@id` is a string; every `@type` a string or an array of strings | ✅ met |
| 0 | prefix-namespaces-terminated | Every prefix maps to an absolute IRI ending in `#` or `/` | ✅ met |
| 0 | relative-ids-plain | Every relative `@id` is a plain path, with no leading `/`, no `.` or `..` segments, and no `?` or `#` | ✅ met |
| 1 | composition-fingerprinted | The TRO's composition, if any, carries a fingerprint | ✅ met |
| 1 | context-and-graph-present | A JSON object with an `@context` and an `@graph` | ✅ met |
| 1 | hashes-well-formed | The TRO's artifact and fingerprint hashes are well-formed sha256 | ✅ met |
| 1 | tro-assembled-by-trs | The TRO names its assembling system, typed as a TRS | ✅ met |
| 1 | tro-top-level-in-graph | The TRO is a top-level member of the `@graph` | ✅ met |
| 1 | trov-terms-known | Every `trov:` name is one TROV defines | ❌ not met |
| *2* | *base-declared* | *The `@context` includes an `@base`* | *not claimed* |
| *2* | *node-ids-present* | *Every node carries an explicit `@id`* | *not claimed* |
## Details
### Unmet expectation: trov-terms-known
Expectation details: Where a property name or a string value carries the trov: prefix, it is a term TROV defines.
| Found | Where | Expectation not met because |
| --- | --- | --- |
| `"trov:IncludesAllOutputData"` | `/@graph/0/trov:hasAttribute/@id` | not a term TROV defines |

----- [5] expectation met: the @type trov:TrustedResearchPerformance is defined by TROV -----

$ report_on instance-type-known.jsonld
{
    "@context": { "trov": "https://w3id.org/trace/trov/0.1/" },
    "@graph": [
        {
            "@id": "tro",
            "@type": "trov:TransparentResearchObject",
            "trov:wasAssembledBy": { "@id": "trs", "@type": "trov:TrustedResearchSystem" },
            "trov:hasPerformance": { "@type": "trov:TrustedResearchPerformance" }
        }
    ]
}

wrote tmp/instance-type-known.md; Tier 0 met, Tier 1 met

# Report
## Candidate Information
|  |  |
| --- | --- |
| Candidate | `instance-type-known.jsonld` |
| Target | Tier 1 — Self-Contained |
| Target declared by | the `--target` option |
## Assessments
### By Representation Tier
| Tier | Name | Description | Status |
| --- | --- | --- | --- |
| 0 | Well-Formed | Valid JSON-LD conforming to the profile TRACE tooling expects | ✅ met |
| 1 | Self-Contained | References within the TRO resolve; its identifiers need not be unique outside it | ✅ met |
| *2* | *Linkable-Data* | *Element identifiers cannot collide with another TRO's; TRO elements can be published* | *not claimed* |
### By Individual Expectation
| Tier | Expectation | Summary | Status |
| --- | --- | --- | --- |
| 0 | base-simple-url | The `@base`, if any, is a simple URL: a host, no user info, dot segments, query or fragment, only URL characters, and a final `/` | ✅ met |
| 0 | base-web-scheme | The `@base`, if any, uses the `https` or `http` scheme | ✅ met |
| 0 | context-well-formed | The root `@context`, if any, has a form JSON-LD allows | ✅ met |
| 0 | disallowed-context-keywords-absent | No keyword in the `@context` other than `@base` | ✅ met |
| 0 | disallowed-node-keywords-absent | No keyword outside the `@context` other than `@context`, `@graph`, `@id` and `@type` | ✅ met |
| 0 | document-rooted-in-nodes | The document is an object or an array of objects | ✅ met |
| 0 | graph-well-formed | The root `@graph`, if any, holds objects, not bare values | ✅ met |
| 0 | ids-and-types-strings | Every `@id` is a string; every `@type` a string or an array of strings | ✅ met |
| 0 | prefix-namespaces-terminated | Every prefix maps to an absolute IRI ending in `#` or `/` | ✅ met |
| 0 | relative-ids-plain | Every relative `@id` is a plain path, with no leading `/`, no `.` or `..` segments, and no `?` or `#` | ✅ met |
| 1 | composition-fingerprinted | The TRO's composition, if any, carries a fingerprint | ✅ met |
| 1 | context-and-graph-present | A JSON object with an `@context` and an `@graph` | ✅ met |
| 1 | hashes-well-formed | The TRO's artifact and fingerprint hashes are well-formed sha256 | ✅ met |
| 1 | tro-assembled-by-trs | The TRO names its assembling system, typed as a TRS | ✅ met |
| 1 | tro-top-level-in-graph | The TRO is a top-level member of the `@graph` | ✅ met |
| 1 | trov-terms-known | Every `trov:` name is one TROV defines | ✅ met |
| *2* | *base-declared* | *The `@context` includes an `@base`* | *not claimed* |
| *2* | *node-ids-present* | *Every node carries an explicit `@id`* | *not claimed* |

----- [6] expectation unmet: the @type trov:TrustedResearchPerformace is not defined by TROV -----

$ report_on instance-type-unknown.jsonld
{
    "@context": { "trov": "https://w3id.org/trace/trov/0.1/" },
    "@graph": [
        {
            "@id": "tro",
            "@type": "trov:TransparentResearchObject",
            "trov:wasAssembledBy": { "@id": "trs", "@type": "trov:TrustedResearchSystem" },
            "trov:hasPerformance": { "@type": "trov:TrustedResearchPerformace" }
        }
    ]
}

wrote tmp/instance-type-unknown.md; Tier 0 met, Tier 1 unmet

# Report
## Candidate Information
|  |  |
| --- | --- |
| Candidate | `instance-type-unknown.jsonld` |
| Target | Tier 1 — Self-Contained |
| Target declared by | the `--target` option |
## Assessments
### By Representation Tier
| Tier | Name | Description | Status |
| --- | --- | --- | --- |
| 0 | Well-Formed | Valid JSON-LD conforming to the profile TRACE tooling expects | ✅ met |
| 1 | Self-Contained | References within the TRO resolve; its identifiers need not be unique outside it | ❌ not met |
| *2* | *Linkable-Data* | *Element identifiers cannot collide with another TRO's; TRO elements can be published* | *not claimed* |
### By Individual Expectation
| Tier | Expectation | Summary | Status |
| --- | --- | --- | --- |
| 0 | base-simple-url | The `@base`, if any, is a simple URL: a host, no user info, dot segments, query or fragment, only URL characters, and a final `/` | ✅ met |
| 0 | base-web-scheme | The `@base`, if any, uses the `https` or `http` scheme | ✅ met |
| 0 | context-well-formed | The root `@context`, if any, has a form JSON-LD allows | ✅ met |
| 0 | disallowed-context-keywords-absent | No keyword in the `@context` other than `@base` | ✅ met |
| 0 | disallowed-node-keywords-absent | No keyword outside the `@context` other than `@context`, `@graph`, `@id` and `@type` | ✅ met |
| 0 | document-rooted-in-nodes | The document is an object or an array of objects | ✅ met |
| 0 | graph-well-formed | The root `@graph`, if any, holds objects, not bare values | ✅ met |
| 0 | ids-and-types-strings | Every `@id` is a string; every `@type` a string or an array of strings | ✅ met |
| 0 | prefix-namespaces-terminated | Every prefix maps to an absolute IRI ending in `#` or `/` | ✅ met |
| 0 | relative-ids-plain | Every relative `@id` is a plain path, with no leading `/`, no `.` or `..` segments, and no `?` or `#` | ✅ met |
| 1 | composition-fingerprinted | The TRO's composition, if any, carries a fingerprint | ✅ met |
| 1 | context-and-graph-present | A JSON object with an `@context` and an `@graph` | ✅ met |
| 1 | hashes-well-formed | The TRO's artifact and fingerprint hashes are well-formed sha256 | ✅ met |
| 1 | tro-assembled-by-trs | The TRO names its assembling system, typed as a TRS | ✅ met |
| 1 | tro-top-level-in-graph | The TRO is a top-level member of the `@graph` | ✅ met |
| 1 | trov-terms-known | Every `trov:` name is one TROV defines | ❌ not met |
| *2* | *base-declared* | *The `@context` includes an `@base`* | *not claimed* |
| *2* | *node-ids-present* | *Every node carries an explicit `@id`* | *not claimed* |
## Details
### Unmet expectation: trov-terms-known
Expectation details: Where a property name or a string value carries the trov: prefix, it is a term TROV defines.
| Found | Where | Expectation not met because |
| --- | --- | --- |
| `"trov:TrustedResearchPerformace"` | `/@graph/0/trov:hasPerformance/@type` | not a term TROV defines |

----- [7] expectation met: the bare value trov:CanIsolateEnvironment is defined by TROV -----

$ report_on instance-value-known.jsonld
{
    "@context": { "trov": "https://w3id.org/trace/trov/0.1/" },
    "@graph": [
        {
            "@id": "tro",
            "@type": "trov:TransparentResearchObject",
            "trov:wasAssembledBy": { "@id": "trs", "@type": "trov:TrustedResearchSystem" },
            "trov:hasCapability": "trov:CanIsolateEnvironment"
        }
    ]
}

wrote tmp/instance-value-known.md; Tier 0 met, Tier 1 met

# Report
## Candidate Information
|  |  |
| --- | --- |
| Candidate | `instance-value-known.jsonld` |
| Target | Tier 1 — Self-Contained |
| Target declared by | the `--target` option |
## Assessments
### By Representation Tier
| Tier | Name | Description | Status |
| --- | --- | --- | --- |
| 0 | Well-Formed | Valid JSON-LD conforming to the profile TRACE tooling expects | ✅ met |
| 1 | Self-Contained | References within the TRO resolve; its identifiers need not be unique outside it | ✅ met |
| *2* | *Linkable-Data* | *Element identifiers cannot collide with another TRO's; TRO elements can be published* | *not claimed* |
### By Individual Expectation
| Tier | Expectation | Summary | Status |
| --- | --- | --- | --- |
| 0 | base-simple-url | The `@base`, if any, is a simple URL: a host, no user info, dot segments, query or fragment, only URL characters, and a final `/` | ✅ met |
| 0 | base-web-scheme | The `@base`, if any, uses the `https` or `http` scheme | ✅ met |
| 0 | context-well-formed | The root `@context`, if any, has a form JSON-LD allows | ✅ met |
| 0 | disallowed-context-keywords-absent | No keyword in the `@context` other than `@base` | ✅ met |
| 0 | disallowed-node-keywords-absent | No keyword outside the `@context` other than `@context`, `@graph`, `@id` and `@type` | ✅ met |
| 0 | document-rooted-in-nodes | The document is an object or an array of objects | ✅ met |
| 0 | graph-well-formed | The root `@graph`, if any, holds objects, not bare values | ✅ met |
| 0 | ids-and-types-strings | Every `@id` is a string; every `@type` a string or an array of strings | ✅ met |
| 0 | prefix-namespaces-terminated | Every prefix maps to an absolute IRI ending in `#` or `/` | ✅ met |
| 0 | relative-ids-plain | Every relative `@id` is a plain path, with no leading `/`, no `.` or `..` segments, and no `?` or `#` | ✅ met |
| 1 | composition-fingerprinted | The TRO's composition, if any, carries a fingerprint | ✅ met |
| 1 | context-and-graph-present | A JSON object with an `@context` and an `@graph` | ✅ met |
| 1 | hashes-well-formed | The TRO's artifact and fingerprint hashes are well-formed sha256 | ✅ met |
| 1 | tro-assembled-by-trs | The TRO names its assembling system, typed as a TRS | ✅ met |
| 1 | tro-top-level-in-graph | The TRO is a top-level member of the `@graph` | ✅ met |
| 1 | trov-terms-known | Every `trov:` name is one TROV defines | ✅ met |
| *2* | *base-declared* | *The `@context` includes an `@base`* | *not claimed* |
| *2* | *node-ids-present* | *Every node carries an explicit `@id`* | *not claimed* |

----- [8] expectation unmet: the bare value trov:CanIsolateNetwork is not defined by TROV -----

$ report_on instance-value-unknown.jsonld
{
    "@context": { "trov": "https://w3id.org/trace/trov/0.1/" },
    "@graph": [
        {
            "@id": "tro",
            "@type": "trov:TransparentResearchObject",
            "trov:wasAssembledBy": { "@id": "trs", "@type": "trov:TrustedResearchSystem" },
            "trov:hasCapability": "trov:CanIsolateNetwork"
        }
    ]
}

wrote tmp/instance-value-unknown.md; Tier 0 met, Tier 1 unmet

# Report
## Candidate Information
|  |  |
| --- | --- |
| Candidate | `instance-value-unknown.jsonld` |
| Target | Tier 1 — Self-Contained |
| Target declared by | the `--target` option |
## Assessments
### By Representation Tier
| Tier | Name | Description | Status |
| --- | --- | --- | --- |
| 0 | Well-Formed | Valid JSON-LD conforming to the profile TRACE tooling expects | ✅ met |
| 1 | Self-Contained | References within the TRO resolve; its identifiers need not be unique outside it | ❌ not met |
| *2* | *Linkable-Data* | *Element identifiers cannot collide with another TRO's; TRO elements can be published* | *not claimed* |
### By Individual Expectation
| Tier | Expectation | Summary | Status |
| --- | --- | --- | --- |
| 0 | base-simple-url | The `@base`, if any, is a simple URL: a host, no user info, dot segments, query or fragment, only URL characters, and a final `/` | ✅ met |
| 0 | base-web-scheme | The `@base`, if any, uses the `https` or `http` scheme | ✅ met |
| 0 | context-well-formed | The root `@context`, if any, has a form JSON-LD allows | ✅ met |
| 0 | disallowed-context-keywords-absent | No keyword in the `@context` other than `@base` | ✅ met |
| 0 | disallowed-node-keywords-absent | No keyword outside the `@context` other than `@context`, `@graph`, `@id` and `@type` | ✅ met |
| 0 | document-rooted-in-nodes | The document is an object or an array of objects | ✅ met |
| 0 | graph-well-formed | The root `@graph`, if any, holds objects, not bare values | ✅ met |
| 0 | ids-and-types-strings | Every `@id` is a string; every `@type` a string or an array of strings | ✅ met |
| 0 | prefix-namespaces-terminated | Every prefix maps to an absolute IRI ending in `#` or `/` | ✅ met |
| 0 | relative-ids-plain | Every relative `@id` is a plain path, with no leading `/`, no `.` or `..` segments, and no `?` or `#` | ✅ met |
| 1 | composition-fingerprinted | The TRO's composition, if any, carries a fingerprint | ✅ met |
| 1 | context-and-graph-present | A JSON object with an `@context` and an `@graph` | ✅ met |
| 1 | hashes-well-formed | The TRO's artifact and fingerprint hashes are well-formed sha256 | ✅ met |
| 1 | tro-assembled-by-trs | The TRO names its assembling system, typed as a TRS | ✅ met |
| 1 | tro-top-level-in-graph | The TRO is a top-level member of the `@graph` | ✅ met |
| 1 | trov-terms-known | Every `trov:` name is one TROV defines | ❌ not met |
| *2* | *base-declared* | *The `@context` includes an `@base`* | *not claimed* |
| *2* | *node-ids-present* | *Every node carries an explicit `@id`* | *not claimed* |
## Details
### Unmet expectation: trov-terms-known
Expectation details: Where a property name or a string value carries the trov: prefix, it is a term TROV defines.
| Found | Where | Expectation not met because |
| --- | --- | --- |
| `"trov:CanIsolateNetwork"` | `/@graph/0/trov:hasCapability` | not a term TROV defines |
