##############################################################################
#
#   tro-checks  ·  demo 19: base-web-scheme (TIER 3 - TRACE-JSON-LD)
#
##############################################################################

----- [1] expectation met: the @base uses the https scheme -------------------

$ report_on instance-https-base.jsonld
{
    "@context": {
        "@base": "https://example.org/tro/2f8a1c94/"
    },
    "@graph": [
        { "@id": "tro" }
    ]
}

wrote tmp/instance-https-base.md; 1 SAFE-JSON met, 2 SAFE-JSON-LD met, 3 TRACE-JSON-LD met

# Report
## Candidate Information
|  |  |
| --- | --- |
| Candidate | `instance-https-base.jsonld` |
| Target | 3 TRACE-JSON-LD |
| Target declared by | the `--target` option |
## Tier Assessments
| Tier | ID | Description | Status |
| --- | --- | --- | --- |
| 1 | SAFE-JSON | JSON that every supported parser reads the same way | ✅ met |
| 2 | SAFE-JSON-LD | JSON-LD that every supported processor reads the same way | ✅ met |
| 3 | TRACE-JSON-LD | JSON-LD in the restricted form the TRACE Specification requires for TRO declarations | ✅ met |
| *4* | *STANDALONE-TRO* | *A TRO declaration with the structure the TRACE Specification requires, whose references resolve within it* | *not claimed* |
| *5* | *LINKABLE-TRO* | *A TRO declaration whose element identifiers cannot collide with another TRO's* | *not claimed* |
## Expectation Findings by Tier
|  |  |  |
| --- | --- | --- |
| **Tier 1 — SAFE-JSON  ✅ met** |  |  |
| Expectation | Summary | Status |
| `duplicate-member-names-absent` | No object repeats a member name | ✅ met |
| `json-parses` | The candidate parses as JSON without errors | ✅ met |
| `lone-surrogates-absent` | No string or member name has an unpaired surrogate | ✅ met |
| `numbers-within-range` | Every number fits a double; every integer is exact | ✅ met |
| `utf8-encoded` | The candidate is UTF-8 | ✅ met |
| **Tier 2 — SAFE-JSON-LD  ✅ met** |  |  |
| Expectation | Summary | Status |
| `containers-absent` | No `@container` in a term definition | ✅ met |
| `context-at-root-only` | The document's only `@context` is the one at its root: no node below it and no term definition within it carries another | ✅ met |
| `context-protection-absent` | No `@protected`, `@propagate` or `@import` in a context | ✅ met |
| `context-well-formed` | The root `@context`, if any, has a form JSON-LD allows | ✅ met |
| `graph-at-root-only` | `@graph` appears only at the root | ✅ met |
| `graph-well-formed` | The root `@graph`, if any, holds objects, not bare values | ✅ met |
| `id-coercion-absent` | No `"@type": "@id"` in a term definition | ✅ met |
| `id-segments-portable` | Every segment of a relative `@id` is a portable name: letters, digits, dots, hyphens and underscores, beginning and ending with a letter or digit | ✅ met |
| `ids-and-types-strings` | Every `@id` is a string; every `@type` a string or an array of strings | ✅ met |
| `vocab-absent` | No `@vocab` in a context | ✅ met |
| **Tier 3 — TRACE-JSON-LD  ✅ met** |  |  |
| Expectation | Summary | Status |
| `base-simple-url` | The `@base`, if any, is a simple URL: a host, no user info, dot segments, query or fragment, only URL characters, and a final `/` | ✅ met |
| `base-web-scheme` | The `@base`, if any, uses the `https` or `http` scheme | ✅ met |
| `context-aliases-absent` | No term definition aliases a property; a term definition holds only a `@type` naming a datatype | ✅ met |
| `context-local` | The `@context` is inline: no string names a remote context | ✅ met |
| `disallowed-context-keywords-absent` | No member of an `@context` is a keyword other than `@base`; what a term definition holds is not a member of the `@context` | ✅ met |
| `disallowed-node-keywords-absent` | No keyword outside the `@context` other than `@context`, `@graph`, `@id` and `@type` | ✅ met |
| `prefix-namespaces-terminated` | Every prefix maps to an absolute IRI ending in `#` or `/` | ✅ met |
| `root-context-and-graph-only` | A JSON object with an `@context`, an `@graph`, and nothing else | ✅ met |
| `types-prefixed-or-absolute` | Every `@type` value is a prefixed or absolute IRI, never a bare name | ✅ met |
| *Tier 4 — STANDALONE-TRO  not claimed* |  |  |
| Expectation | Summary | Status |
| *`artifact-hashes-present`* | *Every artifact in the composition carries a `trov:hash`, one hash or an array of at least one* | *not claimed* |
| *`composition-has-fingerprint`* | *The TRO's composition, if any, carries one fingerprint, which carries one hash* | *not claimed* |
| *`composition-identifies-artifacts`* | *The TRO's composition, if any, names at least one artifact in a `trov:hasArtifact` array* | *not claimed* |
| *`core-prefixes-pinned`* | *`trov` is declared and is the only prefix for a TROV namespace; `rdf`, `rdfs` and `schema` prefixes, if declared, are the standard ones* | *not claimed* |
| *`creators-person-or-organization`* | *The TRO's `schema:creator`, if present, is a node typed `schema:Person` or `schema:Organization`, never a string* | *not claimed* |
| *`hash-algorithms-permitted`* | *Every hash names an algorithm TRACE permits: a collision-resistant digest from the SHA-2, SHA-3 or BLAKE families* | *not claimed* |
| *`hash-values-correct-length`* | *Every hash value has the length its algorithm produces* | *not claimed* |
| *`hash-values-lowercase-hex`* | *Every hash carries its value as a string of lowercase hexadecimal digits* | *not claimed* |
| *`mime-types-two-part`* | *Every artifact's `trov:mimeType` is a string of the form `type/subtype`* | *not claimed* |
| *`times-iso-8601`* | *Every `trov:startedAtTime` and `trov:endedAtTime` is an ISO 8601 date-time; every `schema:dateCreated` an ISO 8601 date or date-time* | *not claimed* |
| *`times-zoned`* | *Every `trov:startedAtTime` and `trov:endedAtTime` carries its time zone* | *not claimed* |
| *`tro-assembled-by-trs`* | *The TRO names its assembling system, typed as a TRS* | *not claimed* |
| *`tro-name-description-text`* | *The TRO's `schema:name` and `schema:description`, if present, are Text: a string or an array of strings* | *not claimed* |
| *`tro-top-level-in-graph`* | *The TRO is a top-level member of the `@graph`* | *not claimed* |
| *`trov-terms-known`* | *Every `trov:` name is one TROV defines* | *not claimed* |
| *`trov-version-known`* | *The TRO declares, in `trov:vocabularyVersion`, a TROV version this checker knows: `0.1`* | *not claimed* |
| *Tier 5 — LINKABLE-TRO  not claimed* |  |  |
| Expectation | Summary | Status |
| *`base-declared`* | *The `@context` includes an `@base`* | *not claimed* |
| *`base-has-path`* | *The `@base` names something below the host, not the host alone* | *not claimed* |
| *`base-host-lowercase`* | *The `@base` host is lowercase* | *not claimed* |
| *`base-host-ownable`* | *The `@base` host is a domain name the minter could hold, not a reserved or documentation name* | *not claimed* |
| *`blank-node-ids-absent`* | *No `@id` is a blank node identifier* | *not claimed* |
| *`node-ids-present`* | *Every node carries an explicit `@id`* | *not claimed* |

----- [2] expectation unmet: the @base is a urn:uuid URI ---------------------

$ report_on instance-urn-base.jsonld
{
    "@context": {
        "@base": "urn:uuid:2f8a1c94-7b3e-4d21-9f6a-0c5e83b7d410"
    },
    "@graph": [
        { "@id": "tro" }
    ]
}

wrote tmp/instance-urn-base.md; 1 SAFE-JSON met, 2 SAFE-JSON-LD met, 3 TRACE-JSON-LD unmet

# Report
## Candidate Information
|  |  |
| --- | --- |
| Candidate | `instance-urn-base.jsonld` |
| Target | 3 TRACE-JSON-LD |
| Target declared by | the `--target` option |
## Tier Assessments
| Tier | ID | Description | Status |
| --- | --- | --- | --- |
| 1 | SAFE-JSON | JSON that every supported parser reads the same way | ✅ met |
| 2 | SAFE-JSON-LD | JSON-LD that every supported processor reads the same way | ✅ met |
| 3 | TRACE-JSON-LD | JSON-LD in the restricted form the TRACE Specification requires for TRO declarations | ❌ not met |
| *4* | *STANDALONE-TRO* | *A TRO declaration with the structure the TRACE Specification requires, whose references resolve within it* | *not claimed* |
| *5* | *LINKABLE-TRO* | *A TRO declaration whose element identifiers cannot collide with another TRO's* | *not claimed* |
## Expectation Findings by Tier
|  |  |  |
| --- | --- | --- |
| **Tier 1 — SAFE-JSON  ✅ met** |  |  |
| Expectation | Summary | Status |
| `duplicate-member-names-absent` | No object repeats a member name | ✅ met |
| `json-parses` | The candidate parses as JSON without errors | ✅ met |
| `lone-surrogates-absent` | No string or member name has an unpaired surrogate | ✅ met |
| `numbers-within-range` | Every number fits a double; every integer is exact | ✅ met |
| `utf8-encoded` | The candidate is UTF-8 | ✅ met |
| **Tier 2 — SAFE-JSON-LD  ✅ met** |  |  |
| Expectation | Summary | Status |
| `containers-absent` | No `@container` in a term definition | ✅ met |
| `context-at-root-only` | The document's only `@context` is the one at its root: no node below it and no term definition within it carries another | ✅ met |
| `context-protection-absent` | No `@protected`, `@propagate` or `@import` in a context | ✅ met |
| `context-well-formed` | The root `@context`, if any, has a form JSON-LD allows | ✅ met |
| `graph-at-root-only` | `@graph` appears only at the root | ✅ met |
| `graph-well-formed` | The root `@graph`, if any, holds objects, not bare values | ✅ met |
| `id-coercion-absent` | No `"@type": "@id"` in a term definition | ✅ met |
| `id-segments-portable` | Every segment of a relative `@id` is a portable name: letters, digits, dots, hyphens and underscores, beginning and ending with a letter or digit | ✅ met |
| `ids-and-types-strings` | Every `@id` is a string; every `@type` a string or an array of strings | ✅ met |
| `vocab-absent` | No `@vocab` in a context | ✅ met |
| **Tier 3 — TRACE-JSON-LD  ❌ not met** |  |  |
| Expectation | Summary | Status |
| `base-simple-url` | The `@base`, if any, is a simple URL: a host, no user info, dot segments, query or fragment, only URL characters, and a final `/` | ❌ not met |
| `base-web-scheme` | The `@base`, if any, uses the `https` or `http` scheme | ❌ not met |
| `context-aliases-absent` | No term definition aliases a property; a term definition holds only a `@type` naming a datatype | ✅ met |
| `context-local` | The `@context` is inline: no string names a remote context | ✅ met |
| `disallowed-context-keywords-absent` | No member of an `@context` is a keyword other than `@base`; what a term definition holds is not a member of the `@context` | ✅ met |
| `disallowed-node-keywords-absent` | No keyword outside the `@context` other than `@context`, `@graph`, `@id` and `@type` | ✅ met |
| `prefix-namespaces-terminated` | Every prefix maps to an absolute IRI ending in `#` or `/` | ✅ met |
| `root-context-and-graph-only` | A JSON object with an `@context`, an `@graph`, and nothing else | ✅ met |
| `types-prefixed-or-absolute` | Every `@type` value is a prefixed or absolute IRI, never a bare name | ✅ met |
| *Tier 4 — STANDALONE-TRO  not claimed* |  |  |
| Expectation | Summary | Status |
| *`artifact-hashes-present`* | *Every artifact in the composition carries a `trov:hash`, one hash or an array of at least one* | *not claimed* |
| *`composition-has-fingerprint`* | *The TRO's composition, if any, carries one fingerprint, which carries one hash* | *not claimed* |
| *`composition-identifies-artifacts`* | *The TRO's composition, if any, names at least one artifact in a `trov:hasArtifact` array* | *not claimed* |
| *`core-prefixes-pinned`* | *`trov` is declared and is the only prefix for a TROV namespace; `rdf`, `rdfs` and `schema` prefixes, if declared, are the standard ones* | *not claimed* |
| *`creators-person-or-organization`* | *The TRO's `schema:creator`, if present, is a node typed `schema:Person` or `schema:Organization`, never a string* | *not claimed* |
| *`hash-algorithms-permitted`* | *Every hash names an algorithm TRACE permits: a collision-resistant digest from the SHA-2, SHA-3 or BLAKE families* | *not claimed* |
| *`hash-values-correct-length`* | *Every hash value has the length its algorithm produces* | *not claimed* |
| *`hash-values-lowercase-hex`* | *Every hash carries its value as a string of lowercase hexadecimal digits* | *not claimed* |
| *`mime-types-two-part`* | *Every artifact's `trov:mimeType` is a string of the form `type/subtype`* | *not claimed* |
| *`times-iso-8601`* | *Every `trov:startedAtTime` and `trov:endedAtTime` is an ISO 8601 date-time; every `schema:dateCreated` an ISO 8601 date or date-time* | *not claimed* |
| *`times-zoned`* | *Every `trov:startedAtTime` and `trov:endedAtTime` carries its time zone* | *not claimed* |
| *`tro-assembled-by-trs`* | *The TRO names its assembling system, typed as a TRS* | *not claimed* |
| *`tro-name-description-text`* | *The TRO's `schema:name` and `schema:description`, if present, are Text: a string or an array of strings* | *not claimed* |
| *`tro-top-level-in-graph`* | *The TRO is a top-level member of the `@graph`* | *not claimed* |
| *`trov-terms-known`* | *Every `trov:` name is one TROV defines* | *not claimed* |
| *`trov-version-known`* | *The TRO declares, in `trov:vocabularyVersion`, a TROV version this checker knows: `0.1`* | *not claimed* |
| *Tier 5 — LINKABLE-TRO  not claimed* |  |  |
| Expectation | Summary | Status |
| *`base-declared`* | *The `@context` includes an `@base`* | *not claimed* |
| *`base-has-path`* | *The `@base` names something below the host, not the host alone* | *not claimed* |
| *`base-host-lowercase`* | *The `@base` host is lowercase* | *not claimed* |
| *`base-host-ownable`* | *The `@base` host is a domain name the minter could hold, not a reserved or documentation name* | *not claimed* |
| *`blank-node-ids-absent`* | *No `@id` is a blank node identifier* | *not claimed* |
| *`node-ids-present`* | *Every node carries an explicit `@id`* | *not claimed* |
## Details
### Unmet expectation: base-simple-url
Expectation details: Where an @context declares an @base as a string, it names a host, carries no user name or password, has no . or .. segments, contains no ? or #, ends in /, contains only characters allowed in a URL, and has a path of portable segments -- letters, digits, dots, hyphens and underscores, each beginning and ending with a letter or digit.
| Found | Where | Expectation not met because |
| --- | --- | --- |
| `"urn:uuid:2f8a1c94-7b3e-4d21-9f6a-0c5e83b7d410"` | `/@context/@base` | an @base names a host |
| `"urn:uuid:2f8a1c94-7b3e-4d21-9f6a-0c5e83b7d410"` | `/@context/@base` | an @base ends in / |
| `"urn:uuid:2f8a1c94-7b3e-4d21-9f6a-0c5e83b7d410"` | `/@context/@base` | a segment of an @base path holds only letters, digits, dots, hyphens and underscores, and begins and ends with a letter or digit |
### Unmet expectation: base-web-scheme
Expectation details: Where an @context declares a non-null @base, it is a string using the https or http scheme.
| Found | Where | Expectation not met because |
| --- | --- | --- |
| `"urn:uuid:2f8a1c94-7b3e-4d21-9f6a-0c5e83b7d410"` | `/@context/@base` | an @base uses the https or http scheme |

----- [3] expectation unmet: the @base is a number ---------------------------

$ report_on instance-number-base.jsonld
{
    "@context": {
        "@base": 42
    },
    "@graph": [
        { "@id": "tro" }
    ]
}

wrote tmp/instance-number-base.md; 1 SAFE-JSON met, 2 SAFE-JSON-LD met, 3 TRACE-JSON-LD unmet

# Report
## Candidate Information
|  |  |
| --- | --- |
| Candidate | `instance-number-base.jsonld` |
| Target | 3 TRACE-JSON-LD |
| Target declared by | the `--target` option |
## Tier Assessments
| Tier | ID | Description | Status |
| --- | --- | --- | --- |
| 1 | SAFE-JSON | JSON that every supported parser reads the same way | ✅ met |
| 2 | SAFE-JSON-LD | JSON-LD that every supported processor reads the same way | ✅ met |
| 3 | TRACE-JSON-LD | JSON-LD in the restricted form the TRACE Specification requires for TRO declarations | ❌ not met |
| *4* | *STANDALONE-TRO* | *A TRO declaration with the structure the TRACE Specification requires, whose references resolve within it* | *not claimed* |
| *5* | *LINKABLE-TRO* | *A TRO declaration whose element identifiers cannot collide with another TRO's* | *not claimed* |
## Expectation Findings by Tier
|  |  |  |
| --- | --- | --- |
| **Tier 1 — SAFE-JSON  ✅ met** |  |  |
| Expectation | Summary | Status |
| `duplicate-member-names-absent` | No object repeats a member name | ✅ met |
| `json-parses` | The candidate parses as JSON without errors | ✅ met |
| `lone-surrogates-absent` | No string or member name has an unpaired surrogate | ✅ met |
| `numbers-within-range` | Every number fits a double; every integer is exact | ✅ met |
| `utf8-encoded` | The candidate is UTF-8 | ✅ met |
| **Tier 2 — SAFE-JSON-LD  ✅ met** |  |  |
| Expectation | Summary | Status |
| `containers-absent` | No `@container` in a term definition | ✅ met |
| `context-at-root-only` | The document's only `@context` is the one at its root: no node below it and no term definition within it carries another | ✅ met |
| `context-protection-absent` | No `@protected`, `@propagate` or `@import` in a context | ✅ met |
| `context-well-formed` | The root `@context`, if any, has a form JSON-LD allows | ✅ met |
| `graph-at-root-only` | `@graph` appears only at the root | ✅ met |
| `graph-well-formed` | The root `@graph`, if any, holds objects, not bare values | ✅ met |
| `id-coercion-absent` | No `"@type": "@id"` in a term definition | ✅ met |
| `id-segments-portable` | Every segment of a relative `@id` is a portable name: letters, digits, dots, hyphens and underscores, beginning and ending with a letter or digit | ✅ met |
| `ids-and-types-strings` | Every `@id` is a string; every `@type` a string or an array of strings | ✅ met |
| `vocab-absent` | No `@vocab` in a context | ✅ met |
| **Tier 3 — TRACE-JSON-LD  ❌ not met** |  |  |
| Expectation | Summary | Status |
| `base-simple-url` | The `@base`, if any, is a simple URL: a host, no user info, dot segments, query or fragment, only URL characters, and a final `/` | ✅ met |
| `base-web-scheme` | The `@base`, if any, uses the `https` or `http` scheme | ❌ not met |
| `context-aliases-absent` | No term definition aliases a property; a term definition holds only a `@type` naming a datatype | ✅ met |
| `context-local` | The `@context` is inline: no string names a remote context | ✅ met |
| `disallowed-context-keywords-absent` | No member of an `@context` is a keyword other than `@base`; what a term definition holds is not a member of the `@context` | ✅ met |
| `disallowed-node-keywords-absent` | No keyword outside the `@context` other than `@context`, `@graph`, `@id` and `@type` | ✅ met |
| `prefix-namespaces-terminated` | Every prefix maps to an absolute IRI ending in `#` or `/` | ✅ met |
| `root-context-and-graph-only` | A JSON object with an `@context`, an `@graph`, and nothing else | ✅ met |
| `types-prefixed-or-absolute` | Every `@type` value is a prefixed or absolute IRI, never a bare name | ✅ met |
| *Tier 4 — STANDALONE-TRO  not claimed* |  |  |
| Expectation | Summary | Status |
| *`artifact-hashes-present`* | *Every artifact in the composition carries a `trov:hash`, one hash or an array of at least one* | *not claimed* |
| *`composition-has-fingerprint`* | *The TRO's composition, if any, carries one fingerprint, which carries one hash* | *not claimed* |
| *`composition-identifies-artifacts`* | *The TRO's composition, if any, names at least one artifact in a `trov:hasArtifact` array* | *not claimed* |
| *`core-prefixes-pinned`* | *`trov` is declared and is the only prefix for a TROV namespace; `rdf`, `rdfs` and `schema` prefixes, if declared, are the standard ones* | *not claimed* |
| *`creators-person-or-organization`* | *The TRO's `schema:creator`, if present, is a node typed `schema:Person` or `schema:Organization`, never a string* | *not claimed* |
| *`hash-algorithms-permitted`* | *Every hash names an algorithm TRACE permits: a collision-resistant digest from the SHA-2, SHA-3 or BLAKE families* | *not claimed* |
| *`hash-values-correct-length`* | *Every hash value has the length its algorithm produces* | *not claimed* |
| *`hash-values-lowercase-hex`* | *Every hash carries its value as a string of lowercase hexadecimal digits* | *not claimed* |
| *`mime-types-two-part`* | *Every artifact's `trov:mimeType` is a string of the form `type/subtype`* | *not claimed* |
| *`times-iso-8601`* | *Every `trov:startedAtTime` and `trov:endedAtTime` is an ISO 8601 date-time; every `schema:dateCreated` an ISO 8601 date or date-time* | *not claimed* |
| *`times-zoned`* | *Every `trov:startedAtTime` and `trov:endedAtTime` carries its time zone* | *not claimed* |
| *`tro-assembled-by-trs`* | *The TRO names its assembling system, typed as a TRS* | *not claimed* |
| *`tro-name-description-text`* | *The TRO's `schema:name` and `schema:description`, if present, are Text: a string or an array of strings* | *not claimed* |
| *`tro-top-level-in-graph`* | *The TRO is a top-level member of the `@graph`* | *not claimed* |
| *`trov-terms-known`* | *Every `trov:` name is one TROV defines* | *not claimed* |
| *`trov-version-known`* | *The TRO declares, in `trov:vocabularyVersion`, a TROV version this checker knows: `0.1`* | *not claimed* |
| *Tier 5 — LINKABLE-TRO  not claimed* |  |  |
| Expectation | Summary | Status |
| *`base-declared`* | *The `@context` includes an `@base`* | *not claimed* |
| *`base-has-path`* | *The `@base` names something below the host, not the host alone* | *not claimed* |
| *`base-host-lowercase`* | *The `@base` host is lowercase* | *not claimed* |
| *`base-host-ownable`* | *The `@base` host is a domain name the minter could hold, not a reserved or documentation name* | *not claimed* |
| *`blank-node-ids-absent`* | *No `@id` is a blank node identifier* | *not claimed* |
| *`node-ids-present`* | *Every node carries an explicit `@id`* | *not claimed* |
## Details
### Unmet expectation: base-web-scheme
Expectation details: Where an @context declares a non-null @base, it is a string using the https or http scheme.
| Found | Where | Expectation not met because |
| --- | --- | --- |
| `42` | `/@context/@base` | an @base is a string |
