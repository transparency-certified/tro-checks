##############################################################################
#
#   tro-checks  ·  demo 02: json-parses (TIER 1 - SAFE-JSON)
#
##############################################################################

----- [1] expectation met: the candidate is a JSON object --------------------

$ report_on instance-object.jsonld
{
    "@context": {},
    "@graph": [
        { "@id": "https://example.org/thing/1" }
    ]
}

wrote tmp/instance-object.md; 1 SAFE-JSON met

# Report
## Candidate Information
|  |  |
| --- | --- |
| Candidate | `instance-object.jsonld` |
| Target | 1 SAFE-JSON |
| Target declared by | the `--target` option |
## Tier Assessments
| Tier | ID | Description | Status |
| --- | --- | --- | --- |
| 1 | SAFE-JSON | JSON that every supported parser reads the same way | ✅ met |
| *2* | *SAFE-JSON-LD* | *JSON-LD that every supported processor reads the same way* | *not claimed* |
| *3* | *TRACE-JSON-LD* | *JSON-LD in the restricted form the TRACE Specification requires for TRO declarations* | *not claimed* |
| *4* | *STANDALONE-TRO* | *A TRO declaration with the structure the TRACE Specification requires, whose references resolve within it* | *not claimed* |
| *5* | *LINKABLE-TRO* | *A TRO declaration whose element identifiers cannot collide with another TRO's* | *not claimed* |
## Expectation Findings by Tier
|  |  |  |
| --- | --- | --- |
| **Tier 1 — SAFE-JSON  ✅ met** |  |  |
| Expectation | Summary | Status |
| `duplicate-member-names-absent` | No object repeats a member name | ✅ met |
| `json-parses` | The candidate parses as JSON without errors | ✅ met |
| `lone-surrogates-absent` | No string or member name has an unpaired surrogate | ✅ met |
| `numbers-within-range` | Every number fits a double; every integer is exact | ✅ met |
| `utf8-encoded` | The candidate is UTF-8 | ✅ met |
| *Tier 2 — SAFE-JSON-LD  not claimed* |  |  |
| Expectation | Summary | Status |
| *`containers-absent`* | *No `@container` in a term definition* | *not claimed* |
| *`context-at-root-only`* | *The document's only `@context` is the one at its root: no node below it and no term definition within it carries another* | *not claimed* |
| *`context-protection-absent`* | *No `@protected`, `@propagate` or `@import` in a context* | *not claimed* |
| *`context-well-formed`* | *The root `@context`, if any, has a form JSON-LD allows* | *not claimed* |
| *`graph-at-root-only`* | *`@graph` appears only at the root* | *not claimed* |
| *`graph-well-formed`* | *The root `@graph`, if any, holds objects, not bare values* | *not claimed* |
| *`id-coercion-absent`* | *No `"@type": "@id"` in a term definition* | *not claimed* |
| *`id-segments-portable`* | *Every segment of a relative `@id` is a portable name: letters, digits, dots, hyphens and underscores, beginning and ending with a letter or digit* | *not claimed* |
| *`ids-and-types-strings`* | *Every `@id` is a string; every `@type` a string or an array of strings* | *not claimed* |
| *`vocab-absent`* | *No `@vocab` in a context* | *not claimed* |
| *Tier 3 — TRACE-JSON-LD  not claimed* |  |  |
| Expectation | Summary | Status |
| *`base-simple-url`* | *The `@base`, if any, is a simple URL: a host, no user info, dot segments, query or fragment, only URL characters, and a final `/`* | *not claimed* |
| *`base-web-scheme`* | *The `@base`, if any, uses the `https` or `http` scheme* | *not claimed* |
| *`context-aliases-absent`* | *No term definition aliases a property; a term definition holds only a `@type` naming a datatype* | *not claimed* |
| *`context-local`* | *The `@context` is inline: no string names a remote context* | *not claimed* |
| *`disallowed-context-keywords-absent`* | *No member of an `@context` is a keyword other than `@base`; what a term definition holds is not a member of the `@context`* | *not claimed* |
| *`disallowed-node-keywords-absent`* | *No keyword outside the `@context` other than `@context`, `@graph`, `@id` and `@type`* | *not claimed* |
| *`prefix-namespaces-terminated`* | *Every prefix maps to an absolute IRI ending in `#` or `/`* | *not claimed* |
| *`root-context-and-graph-only`* | *A JSON object with an `@context`, an `@graph`, and nothing else* | *not claimed* |
| *`types-prefixed-or-absolute`* | *Every `@type` value is a prefixed or absolute IRI, never a bare name* | *not claimed* |
| *Tier 4 — STANDALONE-TRO  not claimed* |  |  |
| Expectation | Summary | Status |
| *`artifact-hashes-present`* | *Every artifact in the composition carries a `trov:hash`, one hash or an array of at least one* | *not claimed* |
| *`composition-has-fingerprint`* | *The TRO's composition, if any, carries one fingerprint, which carries one hash* | *not claimed* |
| *`composition-identifies-artifacts`* | *The TRO's composition, if any, names at least one artifact in a `trov:hasArtifact` array* | *not claimed* |
| *`core-prefixes-pinned`* | *`trov` is declared and is the only prefix for a TROV namespace; `rdf`, `rdfs` and `schema` prefixes, if declared, are the standard ones* | *not claimed* |
| *`creators-person-or-organization`* | *The TRO's `schema:creator`, if present, is a node typed `schema:Person` or `schema:Organization`, never a string* | *not claimed* |
| *`custom-term-superclasses-extensible`* | *Every custom term extends `trov:TRSCapabilityType` or `trov:TRPAttributeType`* | *not claimed* |
| *`custom-terms-not-trov`* | *Every `trov:customTerm` entry declares a term outside the TROV namespace* | *not claimed* |
| *`gpg-signing-key-present`* | *A TRO signed with `trov:GPGSigning` gives its TRS a `trov:publicKey`* | *not claimed* |
| *`hash-algorithms-permitted`* | *Every hash names an algorithm TRACE permits: a collision-resistant digest from the SHA-2, SHA-3 or BLAKE families* | *not claimed* |
| *`hash-values-correct-form`* | *Every hash value is lowercase hexadecimal of the length its algorithm produces* | *not claimed* |
| *`mime-types-two-part`* | *Every artifact's `trov:mimeType` is a string of the form `type/subtype`* | *not claimed* |
| *`times-iso-8601`* | *Every `trov:startedAtTime` and `trov:endedAtTime` is an ISO 8601 date-time; every `schema:dateCreated` an ISO 8601 date or date-time* | *not claimed* |
| *`times-zoned`* | *Every `trov:startedAtTime` and `trov:endedAtTime` carries its time zone* | *not claimed* |
| *`tro-assembled-by-trs`* | *The TRO names its assembling system, typed as a TRS* | *not claimed* |
| *`tro-name-description-text`* | *The TRO's `schema:name` and `schema:description`, if present, are Text: a string or an array of strings* | *not claimed* |
| *`tro-top-level-in-graph`* | *The TRO is a top-level member of the `@graph`* | *not claimed* |
| *`trov-capabilities-predefined`* | *A capability's `trov:` type is one TROV predefines* | *not claimed* |
| *`trov-performance-attributes-predefined`* | *A performance attribute's `trov:` type is one TROV predefines* | *not claimed* |
| *`trov-signing-mechanisms-predefined`* | *A signing mechanism is identified by reference, and a `trov:` one is one TROV predefines* | *not claimed* |
| *`trov-terms-known`* | *Every `trov:` name is one TROV defines* | *not claimed* |
| *`trov-tro-attributes-predefined`* | *A TRO attribute's `trov:` type is one TROV predefines* | *not claimed* |
| *`trov-version-known`* | *The TRO declares, in `trov:vocabularyVersion`, a TROV version this checker knows: `0.1`* | *not claimed* |
| *Tier 5 — LINKABLE-TRO  not claimed* |  |  |
| Expectation | Summary | Status |
| *`base-declared`* | *The `@context` includes an `@base`* | *not claimed* |
| *`base-has-path`* | *The `@base` names something below the host, not the host alone* | *not claimed* |
| *`base-host-lowercase`* | *The `@base` host is lowercase* | *not claimed* |
| *`base-host-ownable`* | *The `@base` host is a domain name the minter could hold, not a reserved or documentation name* | *not claimed* |
| *`blank-node-ids-absent`* | *No `@id` is a blank node identifier* | *not claimed* |
| *`node-ids-present`* | *Every node carries an explicit `@id`* | *not claimed* |

----- [2] expectation met: the candidate is a JSON number, which is JSON though not JSON-LD -----

$ report_on instance-number.jsonld
42

wrote tmp/instance-number.md; 1 SAFE-JSON met

# Report
## Candidate Information
|  |  |
| --- | --- |
| Candidate | `instance-number.jsonld` |
| Target | 1 SAFE-JSON |
| Target declared by | the `--target` option |
## Tier Assessments
| Tier | ID | Description | Status |
| --- | --- | --- | --- |
| 1 | SAFE-JSON | JSON that every supported parser reads the same way | ✅ met |
| *2* | *SAFE-JSON-LD* | *JSON-LD that every supported processor reads the same way* | *not claimed* |
| *3* | *TRACE-JSON-LD* | *JSON-LD in the restricted form the TRACE Specification requires for TRO declarations* | *not claimed* |
| *4* | *STANDALONE-TRO* | *A TRO declaration with the structure the TRACE Specification requires, whose references resolve within it* | *not claimed* |
| *5* | *LINKABLE-TRO* | *A TRO declaration whose element identifiers cannot collide with another TRO's* | *not claimed* |
## Expectation Findings by Tier
|  |  |  |
| --- | --- | --- |
| **Tier 1 — SAFE-JSON  ✅ met** |  |  |
| Expectation | Summary | Status |
| `duplicate-member-names-absent` | No object repeats a member name | ✅ met |
| `json-parses` | The candidate parses as JSON without errors | ✅ met |
| `lone-surrogates-absent` | No string or member name has an unpaired surrogate | ✅ met |
| `numbers-within-range` | Every number fits a double; every integer is exact | ✅ met |
| `utf8-encoded` | The candidate is UTF-8 | ✅ met |
| *Tier 2 — SAFE-JSON-LD  not claimed* |  |  |
| Expectation | Summary | Status |
| *`containers-absent`* | *No `@container` in a term definition* | *not claimed* |
| *`context-at-root-only`* | *The document's only `@context` is the one at its root: no node below it and no term definition within it carries another* | *not claimed* |
| *`context-protection-absent`* | *No `@protected`, `@propagate` or `@import` in a context* | *not claimed* |
| *`context-well-formed`* | *The root `@context`, if any, has a form JSON-LD allows* | *not claimed* |
| *`graph-at-root-only`* | *`@graph` appears only at the root* | *not claimed* |
| *`graph-well-formed`* | *The root `@graph`, if any, holds objects, not bare values* | *not claimed* |
| *`id-coercion-absent`* | *No `"@type": "@id"` in a term definition* | *not claimed* |
| *`id-segments-portable`* | *Every segment of a relative `@id` is a portable name: letters, digits, dots, hyphens and underscores, beginning and ending with a letter or digit* | *not claimed* |
| *`ids-and-types-strings`* | *Every `@id` is a string; every `@type` a string or an array of strings* | *not claimed* |
| *`vocab-absent`* | *No `@vocab` in a context* | *not claimed* |
| *Tier 3 — TRACE-JSON-LD  not claimed* |  |  |
| Expectation | Summary | Status |
| *`base-simple-url`* | *The `@base`, if any, is a simple URL: a host, no user info, dot segments, query or fragment, only URL characters, and a final `/`* | *not claimed* |
| *`base-web-scheme`* | *The `@base`, if any, uses the `https` or `http` scheme* | *not claimed* |
| *`context-aliases-absent`* | *No term definition aliases a property; a term definition holds only a `@type` naming a datatype* | *not claimed* |
| *`context-local`* | *The `@context` is inline: no string names a remote context* | *not claimed* |
| *`disallowed-context-keywords-absent`* | *No member of an `@context` is a keyword other than `@base`; what a term definition holds is not a member of the `@context`* | *not claimed* |
| *`disallowed-node-keywords-absent`* | *No keyword outside the `@context` other than `@context`, `@graph`, `@id` and `@type`* | *not claimed* |
| *`prefix-namespaces-terminated`* | *Every prefix maps to an absolute IRI ending in `#` or `/`* | *not claimed* |
| *`root-context-and-graph-only`* | *A JSON object with an `@context`, an `@graph`, and nothing else* | *not claimed* |
| *`types-prefixed-or-absolute`* | *Every `@type` value is a prefixed or absolute IRI, never a bare name* | *not claimed* |
| *Tier 4 — STANDALONE-TRO  not claimed* |  |  |
| Expectation | Summary | Status |
| *`artifact-hashes-present`* | *Every artifact in the composition carries a `trov:hash`, one hash or an array of at least one* | *not claimed* |
| *`composition-has-fingerprint`* | *The TRO's composition, if any, carries one fingerprint, which carries one hash* | *not claimed* |
| *`composition-identifies-artifacts`* | *The TRO's composition, if any, names at least one artifact in a `trov:hasArtifact` array* | *not claimed* |
| *`core-prefixes-pinned`* | *`trov` is declared and is the only prefix for a TROV namespace; `rdf`, `rdfs` and `schema` prefixes, if declared, are the standard ones* | *not claimed* |
| *`creators-person-or-organization`* | *The TRO's `schema:creator`, if present, is a node typed `schema:Person` or `schema:Organization`, never a string* | *not claimed* |
| *`custom-term-superclasses-extensible`* | *Every custom term extends `trov:TRSCapabilityType` or `trov:TRPAttributeType`* | *not claimed* |
| *`custom-terms-not-trov`* | *Every `trov:customTerm` entry declares a term outside the TROV namespace* | *not claimed* |
| *`gpg-signing-key-present`* | *A TRO signed with `trov:GPGSigning` gives its TRS a `trov:publicKey`* | *not claimed* |
| *`hash-algorithms-permitted`* | *Every hash names an algorithm TRACE permits: a collision-resistant digest from the SHA-2, SHA-3 or BLAKE families* | *not claimed* |
| *`hash-values-correct-form`* | *Every hash value is lowercase hexadecimal of the length its algorithm produces* | *not claimed* |
| *`mime-types-two-part`* | *Every artifact's `trov:mimeType` is a string of the form `type/subtype`* | *not claimed* |
| *`times-iso-8601`* | *Every `trov:startedAtTime` and `trov:endedAtTime` is an ISO 8601 date-time; every `schema:dateCreated` an ISO 8601 date or date-time* | *not claimed* |
| *`times-zoned`* | *Every `trov:startedAtTime` and `trov:endedAtTime` carries its time zone* | *not claimed* |
| *`tro-assembled-by-trs`* | *The TRO names its assembling system, typed as a TRS* | *not claimed* |
| *`tro-name-description-text`* | *The TRO's `schema:name` and `schema:description`, if present, are Text: a string or an array of strings* | *not claimed* |
| *`tro-top-level-in-graph`* | *The TRO is a top-level member of the `@graph`* | *not claimed* |
| *`trov-capabilities-predefined`* | *A capability's `trov:` type is one TROV predefines* | *not claimed* |
| *`trov-performance-attributes-predefined`* | *A performance attribute's `trov:` type is one TROV predefines* | *not claimed* |
| *`trov-signing-mechanisms-predefined`* | *A signing mechanism is identified by reference, and a `trov:` one is one TROV predefines* | *not claimed* |
| *`trov-terms-known`* | *Every `trov:` name is one TROV defines* | *not claimed* |
| *`trov-tro-attributes-predefined`* | *A TRO attribute's `trov:` type is one TROV predefines* | *not claimed* |
| *`trov-version-known`* | *The TRO declares, in `trov:vocabularyVersion`, a TROV version this checker knows: `0.1`* | *not claimed* |
| *Tier 5 — LINKABLE-TRO  not claimed* |  |  |
| Expectation | Summary | Status |
| *`base-declared`* | *The `@context` includes an `@base`* | *not claimed* |
| *`base-has-path`* | *The `@base` names something below the host, not the host alone* | *not claimed* |
| *`base-host-lowercase`* | *The `@base` host is lowercase* | *not claimed* |
| *`base-host-ownable`* | *The `@base` host is a domain name the minter could hold, not a reserved or documentation name* | *not claimed* |
| *`blank-node-ids-absent`* | *No `@id` is a blank node identifier* | *not claimed* |
| *`node-ids-present`* | *Every node carries an explicit `@id`* | *not claimed* |

----- [3] expectation unmet: the candidate is a bare word --------------------

$ report_on instance-bare-word.jsonld
foo

wrote tmp/instance-bare-word.md; 1 SAFE-JSON unmet

# Report
## Candidate Information
|  |  |
| --- | --- |
| Candidate | `instance-bare-word.jsonld` |
| Target | 1 SAFE-JSON |
| Target declared by | the `--target` option |
## Tier Assessments
| Tier | ID | Description | Status |
| --- | --- | --- | --- |
| 1 | SAFE-JSON | JSON that every supported parser reads the same way | ❌ not met |
| *2* | *SAFE-JSON-LD* | *JSON-LD that every supported processor reads the same way* | *not claimed* |
| *3* | *TRACE-JSON-LD* | *JSON-LD in the restricted form the TRACE Specification requires for TRO declarations* | *not claimed* |
| *4* | *STANDALONE-TRO* | *A TRO declaration with the structure the TRACE Specification requires, whose references resolve within it* | *not claimed* |
| *5* | *LINKABLE-TRO* | *A TRO declaration whose element identifiers cannot collide with another TRO's* | *not claimed* |
## Expectation Findings by Tier
|  |  |  |
| --- | --- | --- |
| **Tier 1 — SAFE-JSON  ❌ not met** |  |  |
| Expectation | Summary | Status |
| `duplicate-member-names-absent` | No object repeats a member name | not assessed |
| `json-parses` | The candidate parses as JSON without errors | ❌ not met |
| `lone-surrogates-absent` | No string or member name has an unpaired surrogate | not assessed |
| `numbers-within-range` | Every number fits a double; every integer is exact | not assessed |
| `utf8-encoded` | The candidate is UTF-8 | ✅ met |
| *Tier 2 — SAFE-JSON-LD  not claimed* |  |  |
| Expectation | Summary | Status |
| *`containers-absent`* | *No `@container` in a term definition* | *not claimed* |
| *`context-at-root-only`* | *The document's only `@context` is the one at its root: no node below it and no term definition within it carries another* | *not claimed* |
| *`context-protection-absent`* | *No `@protected`, `@propagate` or `@import` in a context* | *not claimed* |
| *`context-well-formed`* | *The root `@context`, if any, has a form JSON-LD allows* | *not claimed* |
| *`graph-at-root-only`* | *`@graph` appears only at the root* | *not claimed* |
| *`graph-well-formed`* | *The root `@graph`, if any, holds objects, not bare values* | *not claimed* |
| *`id-coercion-absent`* | *No `"@type": "@id"` in a term definition* | *not claimed* |
| *`id-segments-portable`* | *Every segment of a relative `@id` is a portable name: letters, digits, dots, hyphens and underscores, beginning and ending with a letter or digit* | *not claimed* |
| *`ids-and-types-strings`* | *Every `@id` is a string; every `@type` a string or an array of strings* | *not claimed* |
| *`vocab-absent`* | *No `@vocab` in a context* | *not claimed* |
| *Tier 3 — TRACE-JSON-LD  not claimed* |  |  |
| Expectation | Summary | Status |
| *`base-simple-url`* | *The `@base`, if any, is a simple URL: a host, no user info, dot segments, query or fragment, only URL characters, and a final `/`* | *not claimed* |
| *`base-web-scheme`* | *The `@base`, if any, uses the `https` or `http` scheme* | *not claimed* |
| *`context-aliases-absent`* | *No term definition aliases a property; a term definition holds only a `@type` naming a datatype* | *not claimed* |
| *`context-local`* | *The `@context` is inline: no string names a remote context* | *not claimed* |
| *`disallowed-context-keywords-absent`* | *No member of an `@context` is a keyword other than `@base`; what a term definition holds is not a member of the `@context`* | *not claimed* |
| *`disallowed-node-keywords-absent`* | *No keyword outside the `@context` other than `@context`, `@graph`, `@id` and `@type`* | *not claimed* |
| *`prefix-namespaces-terminated`* | *Every prefix maps to an absolute IRI ending in `#` or `/`* | *not claimed* |
| *`root-context-and-graph-only`* | *A JSON object with an `@context`, an `@graph`, and nothing else* | *not claimed* |
| *`types-prefixed-or-absolute`* | *Every `@type` value is a prefixed or absolute IRI, never a bare name* | *not claimed* |
| *Tier 4 — STANDALONE-TRO  not claimed* |  |  |
| Expectation | Summary | Status |
| *`artifact-hashes-present`* | *Every artifact in the composition carries a `trov:hash`, one hash or an array of at least one* | *not claimed* |
| *`composition-has-fingerprint`* | *The TRO's composition, if any, carries one fingerprint, which carries one hash* | *not claimed* |
| *`composition-identifies-artifacts`* | *The TRO's composition, if any, names at least one artifact in a `trov:hasArtifact` array* | *not claimed* |
| *`core-prefixes-pinned`* | *`trov` is declared and is the only prefix for a TROV namespace; `rdf`, `rdfs` and `schema` prefixes, if declared, are the standard ones* | *not claimed* |
| *`creators-person-or-organization`* | *The TRO's `schema:creator`, if present, is a node typed `schema:Person` or `schema:Organization`, never a string* | *not claimed* |
| *`custom-term-superclasses-extensible`* | *Every custom term extends `trov:TRSCapabilityType` or `trov:TRPAttributeType`* | *not claimed* |
| *`custom-terms-not-trov`* | *Every `trov:customTerm` entry declares a term outside the TROV namespace* | *not claimed* |
| *`gpg-signing-key-present`* | *A TRO signed with `trov:GPGSigning` gives its TRS a `trov:publicKey`* | *not claimed* |
| *`hash-algorithms-permitted`* | *Every hash names an algorithm TRACE permits: a collision-resistant digest from the SHA-2, SHA-3 or BLAKE families* | *not claimed* |
| *`hash-values-correct-form`* | *Every hash value is lowercase hexadecimal of the length its algorithm produces* | *not claimed* |
| *`mime-types-two-part`* | *Every artifact's `trov:mimeType` is a string of the form `type/subtype`* | *not claimed* |
| *`times-iso-8601`* | *Every `trov:startedAtTime` and `trov:endedAtTime` is an ISO 8601 date-time; every `schema:dateCreated` an ISO 8601 date or date-time* | *not claimed* |
| *`times-zoned`* | *Every `trov:startedAtTime` and `trov:endedAtTime` carries its time zone* | *not claimed* |
| *`tro-assembled-by-trs`* | *The TRO names its assembling system, typed as a TRS* | *not claimed* |
| *`tro-name-description-text`* | *The TRO's `schema:name` and `schema:description`, if present, are Text: a string or an array of strings* | *not claimed* |
| *`tro-top-level-in-graph`* | *The TRO is a top-level member of the `@graph`* | *not claimed* |
| *`trov-capabilities-predefined`* | *A capability's `trov:` type is one TROV predefines* | *not claimed* |
| *`trov-performance-attributes-predefined`* | *A performance attribute's `trov:` type is one TROV predefines* | *not claimed* |
| *`trov-signing-mechanisms-predefined`* | *A signing mechanism is identified by reference, and a `trov:` one is one TROV predefines* | *not claimed* |
| *`trov-terms-known`* | *Every `trov:` name is one TROV defines* | *not claimed* |
| *`trov-tro-attributes-predefined`* | *A TRO attribute's `trov:` type is one TROV predefines* | *not claimed* |
| *`trov-version-known`* | *The TRO declares, in `trov:vocabularyVersion`, a TROV version this checker knows: `0.1`* | *not claimed* |
| *Tier 5 — LINKABLE-TRO  not claimed* |  |  |
| Expectation | Summary | Status |
| *`base-declared`* | *The `@context` includes an `@base`* | *not claimed* |
| *`base-has-path`* | *The `@base` names something below the host, not the host alone* | *not claimed* |
| *`base-host-lowercase`* | *The `@base` host is lowercase* | *not claimed* |
| *`base-host-ownable`* | *The `@base` host is a domain name the minter could hold, not a reserved or documentation name* | *not claimed* |
| *`blank-node-ids-absent`* | *No `@id` is a blank node identifier* | *not claimed* |
| *`node-ids-present`* | *Every node carries an explicit `@id`* | *not claimed* |
## Diagnostics for Each Unmet Expectation
### Unmet expectation: json-parses
Expectation details: The candidate's text parses as a single JSON value, with nothing before or after it, without errors.
| Found | Where | Expectation not met because |
| --- | --- | --- |
|  | the document | the candidate is not JSON |

----- [4] expectation unmet: an array ends with a trailing comma -------------

$ report_on instance-trailing-comma.jsonld
{
    "@context": {},
    "@graph": [
        { "@id": "https://example.org/thing/1" },
    ]
}

wrote tmp/instance-trailing-comma.md; 1 SAFE-JSON unmet

# Report
## Candidate Information
|  |  |
| --- | --- |
| Candidate | `instance-trailing-comma.jsonld` |
| Target | 1 SAFE-JSON |
| Target declared by | the `--target` option |
## Tier Assessments
| Tier | ID | Description | Status |
| --- | --- | --- | --- |
| 1 | SAFE-JSON | JSON that every supported parser reads the same way | ❌ not met |
| *2* | *SAFE-JSON-LD* | *JSON-LD that every supported processor reads the same way* | *not claimed* |
| *3* | *TRACE-JSON-LD* | *JSON-LD in the restricted form the TRACE Specification requires for TRO declarations* | *not claimed* |
| *4* | *STANDALONE-TRO* | *A TRO declaration with the structure the TRACE Specification requires, whose references resolve within it* | *not claimed* |
| *5* | *LINKABLE-TRO* | *A TRO declaration whose element identifiers cannot collide with another TRO's* | *not claimed* |
## Expectation Findings by Tier
|  |  |  |
| --- | --- | --- |
| **Tier 1 — SAFE-JSON  ❌ not met** |  |  |
| Expectation | Summary | Status |
| `duplicate-member-names-absent` | No object repeats a member name | not assessed |
| `json-parses` | The candidate parses as JSON without errors | ❌ not met |
| `lone-surrogates-absent` | No string or member name has an unpaired surrogate | not assessed |
| `numbers-within-range` | Every number fits a double; every integer is exact | not assessed |
| `utf8-encoded` | The candidate is UTF-8 | ✅ met |
| *Tier 2 — SAFE-JSON-LD  not claimed* |  |  |
| Expectation | Summary | Status |
| *`containers-absent`* | *No `@container` in a term definition* | *not claimed* |
| *`context-at-root-only`* | *The document's only `@context` is the one at its root: no node below it and no term definition within it carries another* | *not claimed* |
| *`context-protection-absent`* | *No `@protected`, `@propagate` or `@import` in a context* | *not claimed* |
| *`context-well-formed`* | *The root `@context`, if any, has a form JSON-LD allows* | *not claimed* |
| *`graph-at-root-only`* | *`@graph` appears only at the root* | *not claimed* |
| *`graph-well-formed`* | *The root `@graph`, if any, holds objects, not bare values* | *not claimed* |
| *`id-coercion-absent`* | *No `"@type": "@id"` in a term definition* | *not claimed* |
| *`id-segments-portable`* | *Every segment of a relative `@id` is a portable name: letters, digits, dots, hyphens and underscores, beginning and ending with a letter or digit* | *not claimed* |
| *`ids-and-types-strings`* | *Every `@id` is a string; every `@type` a string or an array of strings* | *not claimed* |
| *`vocab-absent`* | *No `@vocab` in a context* | *not claimed* |
| *Tier 3 — TRACE-JSON-LD  not claimed* |  |  |
| Expectation | Summary | Status |
| *`base-simple-url`* | *The `@base`, if any, is a simple URL: a host, no user info, dot segments, query or fragment, only URL characters, and a final `/`* | *not claimed* |
| *`base-web-scheme`* | *The `@base`, if any, uses the `https` or `http` scheme* | *not claimed* |
| *`context-aliases-absent`* | *No term definition aliases a property; a term definition holds only a `@type` naming a datatype* | *not claimed* |
| *`context-local`* | *The `@context` is inline: no string names a remote context* | *not claimed* |
| *`disallowed-context-keywords-absent`* | *No member of an `@context` is a keyword other than `@base`; what a term definition holds is not a member of the `@context`* | *not claimed* |
| *`disallowed-node-keywords-absent`* | *No keyword outside the `@context` other than `@context`, `@graph`, `@id` and `@type`* | *not claimed* |
| *`prefix-namespaces-terminated`* | *Every prefix maps to an absolute IRI ending in `#` or `/`* | *not claimed* |
| *`root-context-and-graph-only`* | *A JSON object with an `@context`, an `@graph`, and nothing else* | *not claimed* |
| *`types-prefixed-or-absolute`* | *Every `@type` value is a prefixed or absolute IRI, never a bare name* | *not claimed* |
| *Tier 4 — STANDALONE-TRO  not claimed* |  |  |
| Expectation | Summary | Status |
| *`artifact-hashes-present`* | *Every artifact in the composition carries a `trov:hash`, one hash or an array of at least one* | *not claimed* |
| *`composition-has-fingerprint`* | *The TRO's composition, if any, carries one fingerprint, which carries one hash* | *not claimed* |
| *`composition-identifies-artifacts`* | *The TRO's composition, if any, names at least one artifact in a `trov:hasArtifact` array* | *not claimed* |
| *`core-prefixes-pinned`* | *`trov` is declared and is the only prefix for a TROV namespace; `rdf`, `rdfs` and `schema` prefixes, if declared, are the standard ones* | *not claimed* |
| *`creators-person-or-organization`* | *The TRO's `schema:creator`, if present, is a node typed `schema:Person` or `schema:Organization`, never a string* | *not claimed* |
| *`custom-term-superclasses-extensible`* | *Every custom term extends `trov:TRSCapabilityType` or `trov:TRPAttributeType`* | *not claimed* |
| *`custom-terms-not-trov`* | *Every `trov:customTerm` entry declares a term outside the TROV namespace* | *not claimed* |
| *`gpg-signing-key-present`* | *A TRO signed with `trov:GPGSigning` gives its TRS a `trov:publicKey`* | *not claimed* |
| *`hash-algorithms-permitted`* | *Every hash names an algorithm TRACE permits: a collision-resistant digest from the SHA-2, SHA-3 or BLAKE families* | *not claimed* |
| *`hash-values-correct-form`* | *Every hash value is lowercase hexadecimal of the length its algorithm produces* | *not claimed* |
| *`mime-types-two-part`* | *Every artifact's `trov:mimeType` is a string of the form `type/subtype`* | *not claimed* |
| *`times-iso-8601`* | *Every `trov:startedAtTime` and `trov:endedAtTime` is an ISO 8601 date-time; every `schema:dateCreated` an ISO 8601 date or date-time* | *not claimed* |
| *`times-zoned`* | *Every `trov:startedAtTime` and `trov:endedAtTime` carries its time zone* | *not claimed* |
| *`tro-assembled-by-trs`* | *The TRO names its assembling system, typed as a TRS* | *not claimed* |
| *`tro-name-description-text`* | *The TRO's `schema:name` and `schema:description`, if present, are Text: a string or an array of strings* | *not claimed* |
| *`tro-top-level-in-graph`* | *The TRO is a top-level member of the `@graph`* | *not claimed* |
| *`trov-capabilities-predefined`* | *A capability's `trov:` type is one TROV predefines* | *not claimed* |
| *`trov-performance-attributes-predefined`* | *A performance attribute's `trov:` type is one TROV predefines* | *not claimed* |
| *`trov-signing-mechanisms-predefined`* | *A signing mechanism is identified by reference, and a `trov:` one is one TROV predefines* | *not claimed* |
| *`trov-terms-known`* | *Every `trov:` name is one TROV defines* | *not claimed* |
| *`trov-tro-attributes-predefined`* | *A TRO attribute's `trov:` type is one TROV predefines* | *not claimed* |
| *`trov-version-known`* | *The TRO declares, in `trov:vocabularyVersion`, a TROV version this checker knows: `0.1`* | *not claimed* |
| *Tier 5 — LINKABLE-TRO  not claimed* |  |  |
| Expectation | Summary | Status |
| *`base-declared`* | *The `@context` includes an `@base`* | *not claimed* |
| *`base-has-path`* | *The `@base` names something below the host, not the host alone* | *not claimed* |
| *`base-host-lowercase`* | *The `@base` host is lowercase* | *not claimed* |
| *`base-host-ownable`* | *The `@base` host is a domain name the minter could hold, not a reserved or documentation name* | *not claimed* |
| *`blank-node-ids-absent`* | *No `@id` is a blank node identifier* | *not claimed* |
| *`node-ids-present`* | *Every node carries an explicit `@id`* | *not claimed* |
## Diagnostics for Each Unmet Expectation
### Unmet expectation: json-parses
Expectation details: The candidate's text parses as a single JSON value, with nothing before or after it, without errors.
| Found | Where | Expectation not met because |
| --- | --- | --- |
|  | the document | the candidate is not JSON |

----- [5] expectation unmet, targeting TRACE-JSON-LD: the tiers above SAFE-JSON are not assessed -----

$ report_at_trace_json_ld_on instance-bare-word.jsonld
foo

wrote tmp/instance-bare-word-at-trace-json-ld.md; 1 SAFE-JSON unmet, 2 SAFE-JSON-LD unmet, 3 TRACE-JSON-LD unmet

# Report
## Candidate Information
|  |  |
| --- | --- |
| Candidate | `instance-bare-word.jsonld` |
| Target | 3 TRACE-JSON-LD |
| Target declared by | the `--target` option |
## Tier Assessments
| Tier | ID | Description | Status |
| --- | --- | --- | --- |
| 1 | SAFE-JSON | JSON that every supported parser reads the same way | ❌ not met |
| 2 | SAFE-JSON-LD | JSON-LD that every supported processor reads the same way | ❌ not met |
| 3 | TRACE-JSON-LD | JSON-LD in the restricted form the TRACE Specification requires for TRO declarations | ❌ not met |
| *4* | *STANDALONE-TRO* | *A TRO declaration with the structure the TRACE Specification requires, whose references resolve within it* | *not claimed* |
| *5* | *LINKABLE-TRO* | *A TRO declaration whose element identifiers cannot collide with another TRO's* | *not claimed* |
## Expectation Findings by Tier
|  |  |  |
| --- | --- | --- |
| **Tier 1 — SAFE-JSON  ❌ not met** |  |  |
| Expectation | Summary | Status |
| `duplicate-member-names-absent` | No object repeats a member name | not assessed |
| `json-parses` | The candidate parses as JSON without errors | ❌ not met |
| `lone-surrogates-absent` | No string or member name has an unpaired surrogate | not assessed |
| `numbers-within-range` | Every number fits a double; every integer is exact | not assessed |
| `utf8-encoded` | The candidate is UTF-8 | ✅ met |
| **Tier 2 — SAFE-JSON-LD  ❌ not met** |  |  |
| Expectation | Summary | Status |
| `containers-absent` | No `@container` in a term definition | not assessed |
| `context-at-root-only` | The document's only `@context` is the one at its root: no node below it and no term definition within it carries another | not assessed |
| `context-protection-absent` | No `@protected`, `@propagate` or `@import` in a context | not assessed |
| `context-well-formed` | The root `@context`, if any, has a form JSON-LD allows | not assessed |
| `graph-at-root-only` | `@graph` appears only at the root | not assessed |
| `graph-well-formed` | The root `@graph`, if any, holds objects, not bare values | not assessed |
| `id-coercion-absent` | No `"@type": "@id"` in a term definition | not assessed |
| `id-segments-portable` | Every segment of a relative `@id` is a portable name: letters, digits, dots, hyphens and underscores, beginning and ending with a letter or digit | not assessed |
| `ids-and-types-strings` | Every `@id` is a string; every `@type` a string or an array of strings | not assessed |
| `vocab-absent` | No `@vocab` in a context | not assessed |
| **Tier 3 — TRACE-JSON-LD  ❌ not met** |  |  |
| Expectation | Summary | Status |
| `base-simple-url` | The `@base`, if any, is a simple URL: a host, no user info, dot segments, query or fragment, only URL characters, and a final `/` | not assessed |
| `base-web-scheme` | The `@base`, if any, uses the `https` or `http` scheme | not assessed |
| `context-aliases-absent` | No term definition aliases a property; a term definition holds only a `@type` naming a datatype | not assessed |
| `context-local` | The `@context` is inline: no string names a remote context | not assessed |
| `disallowed-context-keywords-absent` | No member of an `@context` is a keyword other than `@base`; what a term definition holds is not a member of the `@context` | not assessed |
| `disallowed-node-keywords-absent` | No keyword outside the `@context` other than `@context`, `@graph`, `@id` and `@type` | not assessed |
| `prefix-namespaces-terminated` | Every prefix maps to an absolute IRI ending in `#` or `/` | not assessed |
| `root-context-and-graph-only` | A JSON object with an `@context`, an `@graph`, and nothing else | not assessed |
| `types-prefixed-or-absolute` | Every `@type` value is a prefixed or absolute IRI, never a bare name | not assessed |
| *Tier 4 — STANDALONE-TRO  not claimed* |  |  |
| Expectation | Summary | Status |
| *`artifact-hashes-present`* | *Every artifact in the composition carries a `trov:hash`, one hash or an array of at least one* | *not claimed* |
| *`composition-has-fingerprint`* | *The TRO's composition, if any, carries one fingerprint, which carries one hash* | *not claimed* |
| *`composition-identifies-artifacts`* | *The TRO's composition, if any, names at least one artifact in a `trov:hasArtifact` array* | *not claimed* |
| *`core-prefixes-pinned`* | *`trov` is declared and is the only prefix for a TROV namespace; `rdf`, `rdfs` and `schema` prefixes, if declared, are the standard ones* | *not claimed* |
| *`creators-person-or-organization`* | *The TRO's `schema:creator`, if present, is a node typed `schema:Person` or `schema:Organization`, never a string* | *not claimed* |
| *`custom-term-superclasses-extensible`* | *Every custom term extends `trov:TRSCapabilityType` or `trov:TRPAttributeType`* | *not claimed* |
| *`custom-terms-not-trov`* | *Every `trov:customTerm` entry declares a term outside the TROV namespace* | *not claimed* |
| *`gpg-signing-key-present`* | *A TRO signed with `trov:GPGSigning` gives its TRS a `trov:publicKey`* | *not claimed* |
| *`hash-algorithms-permitted`* | *Every hash names an algorithm TRACE permits: a collision-resistant digest from the SHA-2, SHA-3 or BLAKE families* | *not claimed* |
| *`hash-values-correct-form`* | *Every hash value is lowercase hexadecimal of the length its algorithm produces* | *not claimed* |
| *`mime-types-two-part`* | *Every artifact's `trov:mimeType` is a string of the form `type/subtype`* | *not claimed* |
| *`times-iso-8601`* | *Every `trov:startedAtTime` and `trov:endedAtTime` is an ISO 8601 date-time; every `schema:dateCreated` an ISO 8601 date or date-time* | *not claimed* |
| *`times-zoned`* | *Every `trov:startedAtTime` and `trov:endedAtTime` carries its time zone* | *not claimed* |
| *`tro-assembled-by-trs`* | *The TRO names its assembling system, typed as a TRS* | *not claimed* |
| *`tro-name-description-text`* | *The TRO's `schema:name` and `schema:description`, if present, are Text: a string or an array of strings* | *not claimed* |
| *`tro-top-level-in-graph`* | *The TRO is a top-level member of the `@graph`* | *not claimed* |
| *`trov-capabilities-predefined`* | *A capability's `trov:` type is one TROV predefines* | *not claimed* |
| *`trov-performance-attributes-predefined`* | *A performance attribute's `trov:` type is one TROV predefines* | *not claimed* |
| *`trov-signing-mechanisms-predefined`* | *A signing mechanism is identified by reference, and a `trov:` one is one TROV predefines* | *not claimed* |
| *`trov-terms-known`* | *Every `trov:` name is one TROV defines* | *not claimed* |
| *`trov-tro-attributes-predefined`* | *A TRO attribute's `trov:` type is one TROV predefines* | *not claimed* |
| *`trov-version-known`* | *The TRO declares, in `trov:vocabularyVersion`, a TROV version this checker knows: `0.1`* | *not claimed* |
| *Tier 5 — LINKABLE-TRO  not claimed* |  |  |
| Expectation | Summary | Status |
| *`base-declared`* | *The `@context` includes an `@base`* | *not claimed* |
| *`base-has-path`* | *The `@base` names something below the host, not the host alone* | *not claimed* |
| *`base-host-lowercase`* | *The `@base` host is lowercase* | *not claimed* |
| *`base-host-ownable`* | *The `@base` host is a domain name the minter could hold, not a reserved or documentation name* | *not claimed* |
| *`blank-node-ids-absent`* | *No `@id` is a blank node identifier* | *not claimed* |
| *`node-ids-present`* | *Every node carries an explicit `@id`* | *not claimed* |
## Diagnostics for Each Unmet Expectation
### Unmet expectation: json-parses
Expectation details: The candidate's text parses as a single JSON value, with nothing before or after it, without errors.
| Found | Where | Expectation not met because |
| --- | --- | --- |
|  | the document | the candidate is not JSON |
